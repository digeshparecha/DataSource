:scheduler:
  :schedule:
    hourly_job:
      cron: "0 0 * * * *"  # Run every hour
      class: "HourlyWorker"
      queue: "default"
    end_of_day_job:
      cron: "0 23 * * *"
      class: "EndOfDayWorker" # Run at End of the day
      queue: default
